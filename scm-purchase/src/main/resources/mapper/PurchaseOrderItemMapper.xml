<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.scm.purchase.mapper.PurchaseOrderItemMapper">

    <resultMap id="purchaseOrderItemResultMap" type="com.example.scm.purchase.entity.PurchaseOrderItem">
        <id property="id" column="id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="purchaseOrderId" column="purchase_order_id"/>
        <result property="materialId" column="material_id"/>
        <result property="planQty" column="plan_qty"/>
        <result property="receivedQty" column="received_qty"/>
        <result property="unitPrice" column="unit_price"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="selectByOrderId" resultMap="purchaseOrderItemResultMap">
        SELECT id, tenant_id, purchase_order_id, material_id, plan_qty, received_qty, unit_price, created_at
        FROM purchase_order_item
        WHERE tenant_id = #{tenantId} AND purchase_order_id = #{purchaseOrderId}
        ORDER BY id ASC
    </select>
</mapper>
